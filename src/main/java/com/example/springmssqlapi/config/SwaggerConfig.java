package com.example.springmssqlapi.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // SpringDoc OpenAPI will auto-configure based on application.properties
    // Basic configuration is handled automatically by springdoc-openapi-starter-webmvc-ui
}
